const fs = require('fs');

// TODO 1: read grades.json (fs.readFile, 'utf8', error-first callback)

  // TODO 2: JSON.parse the file content into an array

  // TODO 3: for each student, compute the average of student.marks, print "name: average"

  // TODO 4: push one new student object onto the array, e.g.
  //   { name: 'New Student', marks: [70, 75, 80] }

  // TODO 5: JSON.stringify the updated array (2-space indent) and fs.writeFile it back to grades.json
