const fs = require('fs');
const path = require('path');

// TODO 1: read products.txt asynchronously (fs.readFile, 'utf8', error-first callback)

  // TODO 2: build the output text: one "Name: Price" line per product, then "TOTAL: <sum>"

  // TODO 3: write that text to report.txt asynchronously (fs.writeFile, error-first callback)

  console.log('Done — check report.txt');
